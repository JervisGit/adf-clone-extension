{
  "_comment": "Config-driven Copy activity source/sink matrix for local run mode. Add new entries here to enable more combinations without changing localRunner.js.",

  "strategies": {
    "adls_stream": {
      "description": "Read file from ADLS Gen2/Blob, write to ADLS Gen2/Blob. Streams text content via ADLSRestClient. Supports: DelimitedText, JSON, Binary (raw bytes), XML, Avro (raw copy only).",
      "formatConversions": {
        "same": true,
        "cross": false,
        "crossReason": "Format conversion (e.g. CSV→Parquet) requires a Spark/Synapse engine. Not supported in local run mode."
      }
    },
    "not_supported": {
      "description": "Requires a live connection (SQL, REST, Salesforce, etc.) — not supported in local run mode."
    }
  },

  "linkedServiceTypeMap": {
    "AzureBlobFS":      "adls",
    "AzureBlobStorage": "blob",
    "AzureSqlDatabase": "sql",
    "AzureSqlDWTable":  "sql",
    "AzureSynapseAnalytics": "sql",
    "SqlServer":        "sql",
    "RestService":      "rest",
    "HttpServer":       "http",
    "AzureDataLakeStore": "adls_gen1"
  },

  "supportedPairs": {
    "adls→adls":  { "strategy": "adls_stream" },
    "adls→blob":  { "strategy": "adls_stream" },
    "blob→adls":  { "strategy": "adls_stream" },
    "blob→blob":  { "strategy": "adls_stream" }
  },

  "unsupportedPairs": {
    "sql→adls":     "SQL source requires live database. Use a real pipeline run.",
    "adls→sql":     "SQL sink requires live database. Use a real pipeline run.",
    "rest→adls":    "REST source requires live HTTP connection.",
    "http→adls":    "HTTP source requires live HTTP connection.",
    "adls_gen1→adls": "ADLS Gen1 is not supported in local run mode."
  }
}
